**Сообщение в Replit (отправить вместе с архивом `newsfire_delta_v1.zip`):**

---

Здравствуйте!

Мы доработали проект *NewsFire* и подготовили дельта-пакет улучшений, который нужно применить поверх текущей версии.

**Что сделано:**

1. Улучшен `index.html` — добавлены SEO-метатеги, Open Graph, доступность (a11y), тёмная тема, кнопки с фокус-стилями.
2. Вынесены стили в `/css/styles.css` (единый стиль для всего сайта, сетка, адаптивность, модальные окна).
3. Созданы отдельные JS-компоненты:

   * `/js/components/header.js` — шапка сайта;
   * `/js/components/footer.js` — подвал;
   * `/js/components/modal.js` — модальные окна.
4. `main.js` — мини‑тест с пояснениями + регистрация Service Worker.
5. Добавлен `manifest.webmanifest` и `sw.js` для офлайн‑кеша (PWA-основа).
6. Вставлено `img/placeholder.png` для превью.

**Что нужно сделать Replit сейчас:**

* Загрузить архив `newsfire_delta_v1.zip` в корень проекта.
* Распаковать его с заменой только перечисленных файлов/папок.
* Перезапустить превью и проверить, чтобы:

  * Навигация работала и выглядела одинаково на всех страницах;
  * Модалка теста открывалась и закрывалась без ошибок;
  * Lighthouse (DevTools → Lighthouse) показывал Performance ≥ 90, Accessibility ≥ 95, SEO ≥ 95.

---

**Мой анализ — что ещё нужно сделать в следующих итерациях:**

1. **Интеграция игр** — подключить рабочие версии всех тренажёров (PASS, эвакуация и т.д.) с логикой, очками, обратной связью.
2. **Калькуляторы** — реализовать расчёт по нормам ППБ РК с пояснениями и ссылками на НПА.
3. **Админ‑панель** — для добавления/редактирования контента (Supabase/Firebase или Node backend).
4. **i18n** — поддержка RU/KZ с переключателем языка.
5. **PWA full** — экран загрузки, иконки, кэширование страниц/игр, офлайн‑режим.
6. **Оптимизация изображений** — замена placeholder'ов на реальные картинки и их WebP‑версии.
7. **CSP** — включить Content Security Policy (после финальной проверки подключений).
8. **Аналитика** — добавить сбор ошибок (Sentry) и счётчик использования игр/тестов.

---

**Пронт для Replit (следующий шаг):**

> Подключите к текущему проекту `/games/pass.html` с полноценной интерактивной логикой PASS: пошаговое выполнение Pull–Aim–Squeeze–Sweep, визуальная анимация, начисление очков, сообщение о результате и возможность перезапуска. Страница должна работать в той же визуальной теме, что и обновлённый `index.html`, и быть полностью адаптивной. После этого интегрируйте ссылку на PASS в раздел «Тренажёры» на главной.
