•	Административная панель и модерация:
•	Привести UI админ-панели к понятному виду: разбить на секции «Пользователи», «Объявления», «Статьи/контент», «Настройки». Возможно, сейчас всё в одном дашборде – лучше сделать меню.
•	В AdminUsersPage: отобразить список пользователей с возможностью поиска по имени/email. Для каждого – действия: бан/разбан, присвоение роли (если предусмотрены роли), просмотр списка его объявлений/постов.
•	В AdminDashboard (если есть главная страница админки): вывести основные метрики – например, количество новых необработанных объявлений, новых пользователей за неделю, и т.п. (необязательно, но удобно).
•	Модерация контента: если планируется, что пользователи могут добавлять статьи или комментарии – предусмотреть их проверку. Например, если комментарии включим, нужна страница в админке «Комментарии» для контроля. Пока такого нет, но заложить структуру.
•	Логи и история: добавить для админов страницу логов (хотя бы элементарно – последние регистрации, входы, добавленные объявления, действия админов). Это поможет отслеживать события.
•	Уведомления для админов: если появляется новое объявление или жалоба – уведомлять админа (например, отдельным значком или email). Чтобы команда быстро реагировала.
•	Единый стиль печати (print.css):
•	Создать/обновить глобальный CSS для печати страниц (print.css), который будет применяться при печати/PDF. В нём для всех калькуляторов и важных страниц оформить вывод: белый фон, чёрный текст, убрать навигацию/меню, оставить только содержимое расчёта или статьи.
•	Для калькуляторов предусмотреть, чтобы на печати выводились все критические данные: введённые значения, результаты, рекомендации, нормативные обоснования. Проверить на каждом калькуляторе, что в режиме печати всё видно (можно сделать отдельный компонент «Passport» с версткой результата для печати).
•	Проверить печать страниц объявлений (вдруг кому-то надо распечатать) – возможно, тоже пригодится, хотя не первостепенно.
•	На печатных стилях можно добавить в футере каждой страницы URL сайта и дату (чтобы в PDF было понятно происхождение).
•	Протестировать, что генерируемые PDF через браузер корректно отображают формулы (особенно в методичке и калькуляторе эвакуации, где формулы с <sub>, <sup> – убедиться, что они не обрезаются).
